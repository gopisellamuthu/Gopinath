#include <stdio.h>

int main()
{
    int i, a, sum=0;
    printf("Enter upper limit: ");
    scanf("%d", &a);
    for(i=1; i<=n; i++)
    {
        sum += i;
    }

    printf("Sum of first %d natural numbers = %d", a, sum);

    return 0;
}
